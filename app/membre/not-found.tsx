import { IncidentIntrouvable } from "@/components/IncidentIntrouvable";

/** Fiche ou page introuvable : le menu de l'espace reste là. */
export default function Introuvable() {
  return <IncidentIntrouvable accueil="/membre" />;
}
