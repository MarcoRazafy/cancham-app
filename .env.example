# Cluster PostgreSQL local au projet (dossier .pgdata), démarré par `npm run db:start`.
# Authentification en « trust » : aucun mot de passe, l'instance n'écoute qu'en local.
DATABASE_URL="postgresql://cancham@127.0.0.1:5434/cancham_connect?schema=public"
